import 'package:flutter/material.dart';

class EShopPage extends StatelessWidget {
  const EShopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('EShop page'),
      ),
      body: Center(
        child: Text('Auth widget'),
      ),
    );
  }
}
